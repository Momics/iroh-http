---
id: "BUG-002"
title: "QUIC connection to LAN IP hangs for 20s on same-machine Deno tests"
status: closed
priority: P1
date: 2026-04-13
area: deno
package: iroh-http-deno
tags: [deno, quic, networking, timeout, test]
---

# [BUG-002] QUIC connection to LAN IP hangs for 20s on same-machine tests

## Summary

`server.addr()` returns the LAN WiFi interface address. Using that address as `directAddrs` for a same-machine client stalls or times out because LAN traffic may be blocked by packet filter rules on macOS.

## Evidence

Observed test output:
```
server addrs: ["192.168.50.16:51908"]
serve + fetch — basic round-trip ... FAILED (20s)
serve + fetch — POST with body ... FAILED (20s)
```

- `packages/iroh-http-deno/test/smoke.test.ts` — networking tests pass LAN addr as `directAddrs`
- `crates/iroh-http-core/src/endpoint.rs` — address reporting returns first non-loopback address

## Impact

Any Deno test or production code that connects two nodes on the same machine using `addr().addrs` will hang or time out unless a loopback address is included.

## Remediation

**Option A (immediate test fix):** Pass `bindAddr: "127.0.0.1:0"` to `createNode` in tests so `addr()` returns a loopback `directAddrs`.
**Option B (library improvement):** When no `directAddrs` are specified and both nodes share the same process, attempt a loopback path first.

Option A is the immediate fix.

## Acceptance criteria

1. Deno smoke tests pass on a macOS developer machine without hanging.
