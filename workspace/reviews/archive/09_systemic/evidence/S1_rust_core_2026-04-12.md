# S1 Rust Core Findings (Delegated)

Date: 2026-04-12

## Findings

1. `R9-S1-01` (`P1`): malformed chunk headers treated as incomplete, enabling unbounded buffering.
2. `R9-S1-02` (`P1`): trailer parsing has no size limit on client/server paths.
3. `R9-S1-03` (`P1`): per-peer fairness enforced per connection, not per in-flight request/stream.
4. `R9-S1-04` (`P2`): 20-bit stream handle index can collide after long-running allocation growth.
5. `R9-S1-05` (`P2`): endpoint backpressure options are configured globally, not per endpoint.
6. `R9-S1-06` (`P2`): error-code table in guideline drifts from implementation classifier.
7. `R9-S1-07` (`P2`): integration tests include sleep-driven and flaky bind behavior.
8. `R9-S1-08` (`P2`): `iroh-http-discovery` public API has no tests.

## Noted Strengths

1. Strong crate separation with `no_std` framing boundary.
2. Uses trusted crates for critical primitives (`qpack`, `base32`, `async-compression`).
3. Core has substantial end-to-end integration coverage.

## Source

Delegated agent output stored in thread notifications on 2026-04-12.
