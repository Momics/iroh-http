# S5 Python Adapter Findings (Delegated)

Date: 2026-04-12

## Findings

1. `R9-S5-01` (`P1`): unsafe raw-pointer lifetime in async mDNS iterator (`__anext__`) may cause UB.
2. `R9-S5-02` (`P1`): public API export inconsistencies (`HandlerResponse` missing, optional mDNS symbol always in `__all__`).
3. `R9-S5-03` (`P1`): documented `IrohRequest.text()` contract is missing.
4. `R9-S5-04` (`P2`): serve handler `str` response body not accepted despite documented contract.
5. `R9-S5-05` (`P2`): stubs are async-inaccurate for key entry points.
6. `R9-S5-06` (`P2`): Python adapter omits trailer semantics exposed by core/bridge.
7. `R9-S5-07` (`P2`): fetch cancellation token wiring/parity missing.

## Recommended Test Additions

1. API surface export and `__all__` consistency test.
2. Serve contract test covering `request.text()` and `str` response body.
3. Parity test for cancellation and trailers.

## Source

Delegated agent output stored in thread notifications on 2026-04-12.
