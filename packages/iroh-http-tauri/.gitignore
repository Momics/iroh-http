# .tauri/tauri-api is populated by `tauri ios init` in the consumer app project.
# It is resolved relative to the app at build time — not managed by this plugin.
.tauri/

# Rust build artifacts (own workspace)
/target/

# TypeScript output
/dist/
